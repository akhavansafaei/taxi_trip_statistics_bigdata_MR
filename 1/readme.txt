Make the script executable if it's not already. You can use the chmod command:
chmod +x task2-run.sh
Now, you can execute the script by providing the required command-line arguments. For example:
./task2-run.sh 3 10
